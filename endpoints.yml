# This file contains the configurations for the action server,
# which runs your custom Python code.
# https://rasa.com/docs/rasa/action-server

action_endpoint:
  url: "http://localhost:5055/webhook"